services:
  - type: web
    name: dokoda
    runtime: node
    buildCommand: npm ci && npm run build
    startCommand: NODE_ENV=production node server/dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
